// create a instance
// write a code in that instance
console.log($);
// onload
$(function () {
  $("#btn").on("click", function (event) {
    console.log("click");
  });

  // $(function/selector).action().action().action().action()
  // $.action()
  /*
        id => #id
        class => .class
        element => h1
     */
});
